-- Хук первого сообщения (2026-07-23): айдол пишет ПЕРВЫМ сразу после того, как его выбрали.
-- До этого айдол молчал, пока фанат не напишет сам — дыра ровно в том месте, где решается,
-- будет ли вторая сессия (метрика: 10 вернувшихся к 31.07, возврат = три сессии подряд).
--
-- Колонка служит одновременно отметкой «уже написали» и ЗАМКОМ идемпотентности.
-- Захват атомарный, на уровне строки:
--   update training_stats set first_msg_at = now() where idol_id = ? and first_msg_at is null
-- Победитель гонки получает 1 строку, все остальные — 0, и второе сообщение не рождается.
-- Это закрывает и повторный вход, и второе устройство, и параллельный тик воркера.
-- При сбое генерации значение возвращается в null, чтобы попытка повторилась позже.
alter table training_stats add column if not exists first_msg_at timestamptz;
