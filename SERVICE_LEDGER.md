# Idolingo — реестр конструкции сервиса

*Живой документ. Что за функция → через что реализовали → насколько устроило → что не так → как решить → когда. Обновляется при каждом значимом решении. Последнее обновление: 19.07.2026.*

Статусы устроенности: ✅ устроило · 🟡 частично · 🔴 не устроило · ⏳ не построено · ⛔ отвергнуто

---

## Ядро продукта (обучение)

| Функция | Через что реализовано | Устроило | Что не так | Как решить | Когда |
|---|---|---|---|---|---|
| Чат с айдолом (движок) | fal.ai `any-llm` → Claude Haiku 4.5; персона из карточки + уровень/близость; история в `chat_messages` | ✅ | — | — | сделано |
| Тон чата (живой друг) | переписан system-промпт: без пояснительных сносок, грамматика только по запросу, 반말 как момент дружбы; **19.07 убраны мета-реплики** («пишу как обычный человек», «я ИИ/бот») — айдол не сравнивает себя с человеком и не объявляет свою природу, просто в образе (юр. запрет на имитацию реальной знаменитости сохранён) | ✅ | — | — | сделано 18-19.07, задеплоено (commit) |
| Близость 존댓말/반말 | `language_pct` в `chat.js` рулит тоном и переходом на 반말 | ✅ | — | — | сделано |
| Уроки корейского | `curriculum.js` (TOPIK I, по образцу King Sejong), последовательная разблокировка | ✅ | прогресс уроков хранится в localStorage, не на сервере | при нужде синхронизации — перенести на сервер | не срочно |
| Проверочные | автогенерация по буквам/словам урока (`buildQuiz`) | ✅ | — | — | сделано |
| Рабочая тетрадь | слова из уроков — авто-пачкой при сдаче; из песен — ручное «+»; localStorage `so_vocab_*` | ✅ | per-word «+» в уроках нет (только пачкой) | добавить выборочное «+», если попросят | по запросу |
| Разбор песен (караоке) | forced alignment (Modal CTC + Demucs) + построчная подсветка | 🟡 | сложная синхронизация текста с аудио | дорабатывается отдельно | в работе |
| **Озвучка уроков (TTS)** | **fal.ai → MiniMax speech-02-hd, `Korean_SweetGirl`, скорость 1.2× + обрезка тишины (ffmpeg); 96 кэш-mp3 в `/tts/`** | 🔴 | **корейский ㄹ не нативный + гласные тянулись (ускорили до 1.2×, но качество всё равно не устраивает); потолок движков на fal** | заменить на нативный движок (Naver CLOVA / Google Chirp3-HD ko-KR / Typecast / Cartesia). Озвучка = кэш-файлы → движок меняется без правок кода | **до публичного запуска; на тест оставлен MiniMax 1.2×** |
| Голос айдола (голосовые сообщения) | ElevenLabs (план) | ⏳ | не построено | премиум-фича, уникальный тембр на айдола | фаза роста |

## Удержание / драйв

| Функция | Через что реализовано | Устроило | Что не так | Как решить | Когда |
|---|---|---|---|---|---|
| Стрик учёбы | миграция 0005 + `engage?action=study`; 🔥 дней подряд на главной | ✅ | — | — | сделано 18.07 |
| «Дней вместе» | дни с создания айдола (второй чип) | ✅ | пассивная метрика (растёт сама) | — | сделано |
| Проактивные сообщения | web-push: service worker + VAPID + Vercel Cron | ⏳ | не построено | построить; Vercel Hobby cron = 1/день (хватает на дневное напоминание), для чаще — Pro/внешний планировщик | следующий инкремент |
| Рейтинг/лига | переиспользовать чарт (`view-feed`/`podium`), XP-лига в стиле Duolingo | ⏳ | не построено; прямой чат незнакомцев отвергнут (модерация + безопасность несовершеннолетних) | строить после теста, при плотности юзеров | фаза 2 |

## Мессенджеры (омниканальность)

| Функция | Через что реализовано | Устроило | Что не так | Как решить | Когда |
|---|---|---|---|---|---|
| Архитектура (Фаза 0) | ядро ответа вынесено в `lib/reply.js` (persona+LLM+запись в тред), `chat.js` = тонкий адаптер; миграция 0006: `linked_accounts`, `link_tokens`, `channel` в chat_messages | ✅ построено 18.07 | — | — | сделано |
| Ingest-эндпоинт | `api/bot.js?action=ingest` — общий приёмник для транспортов без вебхука (Discord-воркер): secret-auth → resolveLinkedIdol → generateReply → reply. Мозг только тут | ✅ проверено 19.07 (200 unlinked) | — | env `INGEST_SECRET` выставлен и подтверждён живым запросом | сделано |
| Discord-бот | **РАБОТАЕТ 24/7 НА VPS (20.07, проверено end-to-end):** DM→воркер на сервере→ingest→ответ айдола. `discord-worker/` Gateway-воркер discord.js v14; привязка OAuth. **ВАЖНО: user-install НЕ доставляет текст ЛС — нужен общий сервер** (mutual guild), иначе messageCreate не приходит | ✅ 24/7, не зависит от ПК | — | Хост: **BitLaunch → DigitalOcean, NYC, Ubuntu 26.04 LTS, 1 GB ($17/мес)**, IP `67.205.175.124`. Воркер в `/opt/idolingo-worker`, systemd `idolingo-worker` (Restart=always, `node --env-file=.env index.js`). Вход по SSH-ключу `idolingo`. Апдейт кода = scp index.js + `systemctl restart`. Баланс BitLaunch $20 ≈ 1 мес — пополнять | ✅ СДЕЛАНО 20.07 |
| LINE-бот | КОД ГОТОВ. `api/bot.js?platform=line` — вебхук с X-Line-Signature → ядро; привязка одноразовым кодом (юзер шлёт код боту). Хост НЕ нужен (вебхук на Vercel = уже 24/7) | 🟡 код, провижининг в работе 19.07 | японский движок работает, UI уроков падает на англ. (мягкий долг) | Сармат заводит LINE Official Account + Messaging API channel → даёт Channel secret + access token → Claude пишет env через VERCEL_TOKEN + webhook URL `https://k-pop-black.vercel.app/api/bot?platform=line` | **⏳ ДЕЛАЕМ СЕЙЧАС** |
| Привязка аккаунта | `link_tokens` (одноразовый код 30 мин) для LINE/Telegram; OAuth-state для Discord. UI: кнопка «Общайся в мессенджере» → оверлей (Discord=1 клик OAuth, LINE=код). `?action=links` показывает подключённое | ✅ построено 19.07 | — | — | сделано |
| Telegram-бот | СНГ — **последним** (низкий ARPU) | ⏳ | не построено | тот же `bot.js`, deep-link `/start <token>` из `link_tokens` | после Discord+LINE |
| WhatsApp | массовая Европа/ЛатАм | ⛔ пока | платный per-message + верификация Meta + нужно юрлицо (нет); проактив вне 72ч окна = платные шаблоны | вернуться после юрлица+выручки | отложено |
| KakaoTalk / iMessage | — | ⛔ | API недоступны (Kakao = корейское юрлицо; iMessage закрыт) | пропускаем | — |

## Инфраструктура / бренд

| Функция | Через что реализовано | Устроило | Что не так | Как решить | Когда |
|---|---|---|---|---|---|
| Хостинг | Vercel, проект `k-pop`, автодеплой из GitHub | ✅ | Hobby: лимит 12 функций, cron 1/день | при росте — Pro | по мере роста |
| Имя/бренд | Idolingo (лого/тайтл/футер обновлены) | 🟡 | домен `idolingo.*` не куплен, живём на `k-pop-black.vercel.app` | купить и подключить домен | до публичного запуска |
| Языки UI/обучения | свой словарь + дропдаун EN/ES/PT/JA/ZH/RU + подпись «язык обучения» | 🟡 | UI переведён полностью только EN/RU; ES/PT/JA/ZH падают на английский (чат учит на всех) | перевести ~120 строк ×4 с вычиткой носителем | до публичного запуска |
| БД / авторизация | Supabase (Seoul), RLS, доступ через `api/*` с service_role | ✅ | — | — | стабильно |
| Оплата | Stripe (`api/pay.js`), тестовые ключи | 🟡 | разовые покупки не начисляют ресурс, UI не связан | доделать связку с UI | до монетизации |
| Портреты айдолов | Midjourney v8, 15 женских портретов | 🟡 | мужских нет (вкладка «Парни» — заглушка) | дорисовать мужских | до расширения |
| TTS-провайдер Azure | — | ⛔ | санкционная стена Microsoft: РФ IP + отказ, даже с сингапурской картой блок по IP | используем fal (работает из РФ) | закрыто 18.07 |
| Видео-клипы (старое ядро) | Kling 3.0 + ElevenLabs Music + Kling LipSync | ⛔/пауза | баги склейки (прыжок причёски), заморозка кадра | вернуться после обкатки обучающего ядра | отложено |

---

*Правила ведения: строку не удалять при решении — менять статус и колонку «Как решить»/«Когда» на факт. Отвергнутое (⛔) оставлять с причиной, чтобы не возвращаться по кругу.*
